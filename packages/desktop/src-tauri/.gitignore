target
gen
Cargo.lock
# Tauri externalBin staging. Populated by ../scripts/copy-cli.mjs
# right before each desktop build; same binaries are also released as
# GitHub Release assets by release-cli-binaries.yml.
binaries
# Bundle-resource staging for the microVM engine binary. Populated
# by ../scripts/copy-vm.mjs when packages/vm has a release build.
vm-bin
# Bundle-resource staging for the repo-built guest api-server.
# Populated by ../scripts/stage-apiserver.mjs in dev/debug pipelines,
# cleared (dir kept) for release builds.
apiserver-bin
