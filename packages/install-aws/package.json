{
  "name": "@appliance.sh/install-aws",
  "version": "1.7.0",
  "description": "Install the Appliance platform on AWS",
  "repository": "https://github.com/appliance-sh/appliance.sh",
  "license": "MIT",
  "author": "Eliot Lim",
  "scripts": {
    "build": "mkdir -p ./dist && cdk synth > dist/appliance-install-aws.yml",
    "deploy": "cdk deploy --all",
    "deploy:direct": "cdk deploy --all -- direct",
    "clean": "rm -rf ./dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@appliance.sh/sdk": "1.7.0",
    "@aws-sdk/client-cloudfront": "^3.955.0",
    "aws-cdk": "^2.1033.0",
    "aws-cdk-lib": "^2.230.0",
    "constructs": "^10.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.1"
  }
}
