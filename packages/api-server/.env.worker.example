APPLIANCE_MODE=worker

# No worker-specific secret. The server re-signs each dispatch with the
# ORIGINAL caller's API key, and the worker verifies against the same
# shared api-key store (S3) via APPLIANCE_BASE_CONFIG below.
APPLIANCE_BASE_CONFIG=
