APPLIANCE_MODE=server

# Function URL of the worker deployment. Capture this from the
# `appliance deploy appliance-api-server worker` output and set it here
# before deploying the server.
WORKER_URL=

# Bootstrap token used to create the first API key (server mode only).
BOOTSTRAP_TOKEN=

APPLIANCE_BASE_CONFIG=
