{
  "name": "@appliance.sh/cli",
  "version": "0.0.0-semantically-versioned",
  "description": "Shell for installing, running, and developing applications on the Appliance platform",
  "license": "MIT",
  "author": "Eliot Lim",
  "type": "commonjs",
  "main": "index.js",
  "bin": {
    "appliance": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf ./dist/",
    "setup": "npm link && npm install --global .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/node": "^24.0.1",
    "commander": "^14.0.0"
  }
}
